<div class="page-header">
  <h1>Create a New Portfolio</h1>
</div>
<div class="pull-left">
	<%= form_for(@portfolio) do |f| %>
		<%= f.label :name %><br />
	  	<%= f.text_field :name %><br />
		 <%= f.submit "Create Portfolio", class: "btn btn-large btn-primary" %>
	<% end %>
</div>